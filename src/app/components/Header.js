import React from 'react';

const Header = () => {
  return (
    <header className="header">
      <div className="header-title">FileMint Dashboard</div>
      <button>Log Out</button>
    </header>
  );
};

export default Header;
